doctype html
html(lang="fr")
  head
    title Page introuvable
    script(src="https://cdn.tailwindcss.com")
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
  body
    main(class="min-h-screen bg-slate-950 text-slate-100")
      .max-w-3xl.mx-auto.px-6.py-16.text-center
        p(class="text-xs uppercase tracking-[0.3em] text-slate-500") 404
        h1(class="mt-4 text-3xl font-semibold tracking-tight") Page introuvable
        p(class="mt-3 text-sm text-slate-400") L’URL demandée n’existe pas ou a été déplacée.
        .mt-6.flex.items-center.justify-center.gap-3
          a(href="/" class="inline-flex items-center rounded-lg border border-slate-700 px-4 py-2 text-sm text-slate-200 hover:bg-slate-900") Retour à l’accueil
          if enableChatUI
            a(href=adminBase class="inline-flex items-center rounded-lg bg-sky-500 px-4 py-2 text-sm font-semibold text-white hover:bg-sky-400") Admin
