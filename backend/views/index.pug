doctype html
html(lang="fr")
  head
    title Chat IA avec RAG
    script(src="https://cdn.tailwindcss.com")
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css")
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    style.
      @keyframes typing {
        0%, 80%, 100% { transform: scale(0.8); opacity: 0.5; }
        40% { transform: scale(1); opacity: 1; }
      }
      .typing-dot { animation: typing 1.4s infinite ease-in-out; }
  body
    main(class="min-h-screen bg-slate-950 text-slate-100")
      .max-w-5xl.mx-auto.px-6.py-10
        header(class="mb-8")
          h1(class="text-3xl font-semibold tracking-tight") Assistant IA - RAG
          p(class="text-slate-400 mt-2") Recherche intelligente dans vos documents

        article(class="bg-slate-900/70 border border-slate-800 rounded-2xl overflow-hidden")
          #messages(class="max-h-[420px] overflow-y-auto space-y-4 p-6")
            .welcome-message.text-center.text-slate-400
              i.fas.fa-robot.text-3xl.mb-3.block.text-slate-500
              p Posez votre question sur l'IA, la gestion de projet ou la ville de Pabios
              p.text-sm.text-slate-500.mt-2 Vos documents seront analysés pour vous fournir une réponse précise

          .input-area(class="border-t border-slate-800 p-4")
            .input-container(class="grid grid-cols-[1fr_auto] gap-3 items-center bg-slate-950 border border-slate-800 rounded-xl px-4 py-3")
              textarea#userInput(
                class="w-full bg-transparent text-slate-100 placeholder:text-slate-500 focus:outline-none resize-none h-11"
                placeholder="Tapez votre question..."
                autocomplete="off"
                maxlength="500"
                rows="2"
              )
              button#sendBtn(type="button" class="inline-flex items-center gap-2 rounded-lg bg-sky-500 px-4 py-2 text-sm font-semibold text-white hover:bg-sky-400")
                i#sendIcon.fas.fa-paper-plane
                span#sendText Envoyer

    script(src="/app.js")
